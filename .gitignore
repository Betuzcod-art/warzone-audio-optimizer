# Artefactos de build
audio-optimizer/build/
audio-optimizer/build-gui/
audio-optimizer/build-nmake/
audio-optimizer/build-vs/
audio-optimizer/build-release/
apo/build/

# Binarios de terceros / descargas grandes (no van al repo, se manejan
# aparte: VB-CABLE se redistribuye vía el instalador, no versionado en git)
VBCABLE_Driver_Pack45/
VBCABLE_Driver_Pack45.zip
installer/package/VBCABLE_Setup_x64.exe
installer/package/payload.zip

# Ejecutables/objetos compilados (main.exe, payload, etc.). El .exe final
# SÍ se distribuye, pero como asset de un GitHub Release, no versionado en
# el árbol de git.
*.exe
*.obj
*.pdb
*.ilk

# Copia local del .sed con la ruta absoluta del checkout (IExpress la exige).
# Es específica de cada máquina, no va al repo.
installer/_local_build.sed

# Binario de terceros incluido en el paquete de distribucion (GPL-3.0,
# se descarga de sourceforge.net/projects/equalizerapo). No se versiona:
# son 11 MB que no son codigo nuestro.
dist/**/EqualizerAPO-*.exe
